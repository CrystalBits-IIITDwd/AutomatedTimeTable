class Course:
    def __init__(self, code, name, faculty, room, hours_per_week, branch, semester):
        self.code = code
        self.name = name
        self.faculty = faculty
        self.room = room
        self.hours_per_week = hours_per_week
        self.branch = branch
        self.semester = semester
